{
  "name": "@bullyoff/calibrate",
  "version": "0.0.0",
  "private": true,
  "description": "Compares simcli aggregate output against real-world targets in docs/rules/calibration-data.md. Tolerance bands, chi-square shape check.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@bullyoff/shared": "workspace:*",
    "@bullyoff/engine": "workspace:*"
  }
}
