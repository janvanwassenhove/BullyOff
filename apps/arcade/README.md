# @bullyoff/arcade

Deliberately empty. Arcade mode is v1.x (BRIEF §3, open question 3). It will
reuse `@bullyoff/engine` and `@bullyoff/render` unchanged; the only new code is
input → tick-stamped commands and a Vue shell. Do not scaffold it before v1.0 ships.
